# Render.com configuration - FORZAR DOCKER
services:
  - type: web
    name: ecoprint-ai
    env: docker
    dockerfilePath: ./Dockerfile.render
    dockerContext: .
    plan: free
    region: frankfurt
    branch: feature/dockerization
    
    # Variables de entorno
    envVars:
      - key: MONGO_URI
        value: mongodb+srv://barbdeveloper11_db_user:2sn26wQR1C4fQuoC@cluster0.n7u5rn.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0
      - key: DB_NAME
        value: ensemble_models
      - key: PYTHONPATH
        value: /app
      - key: PYTHONUNBUFFERED
        value: "1"
    
    # Configuración de salud
    healthCheckPath: /health
    
    # Configuración de red
    httpPort: 8000
    
    # Auto-deploy
    autoDeploy: true